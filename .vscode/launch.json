{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "▶ Run OSINT Tool",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/osint.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}